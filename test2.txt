功能 fibonacci(n) {
    數位 a <- 0;
    數位 b <- 1;
    數位 counter <- 0;
    
    儘管 counter < n {
        列印(a);
        數位 temp <- a;
        a <- b;
        b <- temp + b;
        counter <- counter + 1;
    }
    返回 a;
}

fibonacci(10);
